# Legacy Scope Notice (`prisma/`)

The Prisma schema in this folder belongs to the legacy Express stack.

For PFE architecture and validation scope, data modeling and migrations are based on:
- `backend/app/models` (SQLAlchemy)
- `backend/alembic/versions` (Alembic)

Do not cite this Prisma schema as the canonical database model in the final report.
