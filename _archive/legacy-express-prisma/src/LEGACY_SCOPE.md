# Legacy Scope Notice (`src/`)

The TypeScript/Express implementation under `src/` is retained for historical compatibility.

For the PFE repository evidence and runtime claims, this path is **not canonical**.
Canonical backend runtime is FastAPI under `backend/app` with SQLAlchemy/Alembic.

Do not use `src/` as architecture reference for chapters 3, 4, and 5.
